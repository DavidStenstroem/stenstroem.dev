import * as React from 'react'
import { RouteComponentProps } from '@reach/router'

export const Register: React.FunctionComponent<RouteComponentProps> = (
  props
): JSX.Element => <div>Register</div>
