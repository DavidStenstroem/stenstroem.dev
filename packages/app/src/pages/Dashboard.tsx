import * as React from 'react'
import { RouteComponentProps } from '@reach/router'

export const Dashboard: React.FunctionComponent<RouteComponentProps> = (
  props
): JSX.Element => <div>Dashboard</div>
