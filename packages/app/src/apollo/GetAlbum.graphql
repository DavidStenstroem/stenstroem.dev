query GetAlbum($slug: String!) {
  getAlbum(slug: $slug) {
    title
    slug
    description
    createdBy {
      name
      slug
    }
    media {
      secureUrl
      width
      height
      resourceType
      format
      publicId
    }
  }
}
